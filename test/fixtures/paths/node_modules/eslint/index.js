// @ts-check

/**
 * @import {ESLint} from 'eslint';
 *
 * @import { MockESLint } from '../../../../../mock.js';
 */

const eslintMock = require('../../../../../__mocks__/eslint')

module.exports = Object.assign({}, eslintMock, {
  ESLint: MockMockESLint
})

/**
 * @this {MockESLint}
 * @param {ESLint.Options} options
 * @returns {MockESLint} MockESLint instance
 */
function MockMockESLint(options) {
  try {
    return eslintMock.ESLint.call(this, options)
  } finally {
    global.__PRETTIER_ESLINT_TEST_STATE__.eslintPath = __filename
  }
}
